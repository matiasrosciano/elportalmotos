import React from 'react'


const SeccionOfertas = () =>(
     <div style={styles.SectionContainer}>
        Usar templates para ofertas carrusel etc
     </div>
)

const styles = {
    SectionContainer: {
        height: '50vh',
        width: '100%',
        backgroundColor: '#cacaca'
    }
};

export default SeccionOfertas